import { BlogsType } from "@/src/interfaces/blogs-interface";
import { HerosType } from "@/src/interfaces/heros-interface";

export interface HeroProps {
    blogs: BlogsType[];
    heros: HerosType[];
    
}